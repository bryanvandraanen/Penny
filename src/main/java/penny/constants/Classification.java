package main.java.penny.constants;

/**
 * Classification enum indicating whether an instance is positive, negative, or invalid (i.e. the instance
 * cannot be classified; there is no sufficient data to classify the instance).
 */
public enum Classification {
    Positive,
    Negative,
    Invalid
}
